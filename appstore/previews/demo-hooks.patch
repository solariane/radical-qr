diff --git a/Radical QR/Features/Generator/LaunchCard.swift b/Radical QR/Features/Generator/LaunchCard.swift
index 0861502..4998d51 100644
--- a/Radical QR/Features/Generator/LaunchCard.swift	
+++ b/Radical QR/Features/Generator/LaunchCard.swift	
@@ -82,6 +82,21 @@ struct LaunchCard: View {
         .onChange(of: scenePhase) { _, phase in
             if phase == .active { pasteboardGeneration += 1 }
         }
+        #if DEBUG
+        // App Preview capture only (applied to a scratch copy by appstore/previews/shoot.sh): runs the
+        // PasteButton closure's exact assignment when the UI test drops a file.
+        .task {
+            guard let path = ProcessInfo.processInfo.environment["DEMO_PASTE_FILE"] else { return }
+            while !Task.isCancelled {
+                if let pasted = try? String(contentsOfFile: path, encoding: .utf8),
+                   !pasted.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty {
+                    try? FileManager.default.removeItem(atPath: path)
+                    withAnimation(.easeOut(duration: 0.2)) { text = pasted }
+                }
+                try? await Task.sleep(for: .milliseconds(50))
+            }
+        }
+        #endif
         #if !os(macOS)
         .fullScreenCover(isPresented: $showScanner) {
             CodeScannerSheet(
diff --git a/Radical QR/Features/Generator/BrandGroupView.swift b/Radical QR/Features/Generator/BrandGroupView.swift
index 22b3725..7371fcf 100644
--- a/Radical QR/Features/Generator/BrandGroupView.swift	
+++ b/Radical QR/Features/Generator/BrandGroupView.swift	
@@ -19,6 +19,20 @@ struct BrandGroupView: View {
             logoRow
             captionRow
         }
+        #if DEBUG
+        // App Preview capture only (scratch copy via shoot.sh): stands in for the Photos pick,
+        // setting the same binding once the UI test drops an image file.
+        .task {
+            guard let path = ProcessInfo.processInfo.environment["DEMO_LOGO_FILE"] else { return }
+            while !Task.isCancelled {
+                if let data = FileManager.default.contents(atPath: path) {
+                    try? FileManager.default.removeItem(atPath: path)
+                    withAnimation { configuration.logoData = data }
+                }
+                try? await Task.sleep(for: .milliseconds(50))
+            }
+        }
+        #endif
     }
 
     // MARK: - Logo
